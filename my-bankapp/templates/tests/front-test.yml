apiVersion: v1
kind: Pod
metadata:
  name: healthtest
  namespace: practicum
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['front-service/actuator/health']
  restartPolicy: Never