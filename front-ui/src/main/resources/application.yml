server:
  port: 8080

spring:
  application:
    name: front

gateway: ${GATEWAY_URL}

resource:
  alias:
    accounts: /accounts
    exchange-generator: /exchange-generator
    transfer: /transfer
    cash: /cash